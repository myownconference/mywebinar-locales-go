# mywebinar-locales-go — project rules + codebase map

> Single source of truth = `AGENTS.md` (read by ZCode/Codex directly; Claude Code loads it via the
> import below). Extends global `~/.claude/CLAUDE.md`. Operational memory lives in
> `~/.claude/projects/<this-project>/memory/` — see `AGENTS.md` → "Session start".

@AGENTS.md
